@page "/tsm/hdd/{CRUCMode}"
@page "/tsm/hdd/{CRUCMode}/{id:int}"

@inherits CRUCPageBase<HDD>

@if (CurrentObject != null && CurrentEditContext != null)
{
    <EditForm EditContext="CurrentEditContext" OnValidSubmit="SaveDummy" OnInvalidSubmit="SaveDummy" style="height:100%; display:flex; flex-direction:column;">
        <DataAnnotationsValidator />
        <MudPaper Class="moog-pg-banner" Elevation="0">
            <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.SpaceBetween">
                <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.FlexStart" Spacing="2">
                    <MudIcon Icon="fad fa-hard-drive" Color="Color.Primary" Size="Size.Large"></MudIcon>
                    <MudText Typo="Typo.h4" Color="@Color.Primary">@HeaderTitle</MudText>
                    @if (CRUCMode == CRUDCModes.READ)
                    {
                        <MudChip Size="Size.Small" Color="Color.Success" Variant="Variant.Outlined" Class="moog-pg-banner-chip" T="string" Style="margin-top:-12px">VIEW</MudChip>
                    }
                    else if (CRUCMode == CRUDCModes.UPDATE)
                    {
                        <MudChip Size="Size.Small" Color="Color.Primary" Variant="Variant.Outlined" Class="moog-pg-banner-chip" T="string" Style="margin-top:-12px">UPDATE</MudChip>
                    }
                    else if (CRUCMode == CRUDCModes.CREATE)
                    {
                        <MudChip Size="Size.Small" Color="Color.Info" Variant="Variant.Outlined" Class="moog-pg-banner-chip" T="string" Style="margin-top:-12px">NEW</MudChip>
                    }
                    else if (CRUCMode == CRUDCModes.CLONE)
                    {
                        <MudChip Size="Size.Small" Color="Color.Info" Variant="Variant.Outlined" Class="moog-pg-banner-chip" T="string" Style="margin-top:-12px">NEW (Clone)</MudChip>
                    }
                </MudStack>
                <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.FlexEnd" Spacing="2">
                    @if (IsViewMode)
                    {
                        <MudButton Variant="Variant.Filled" Size="Size.Small" DropShadow="false" Class="moog-mudbutton" Color="Color.Default"
                                   Disabled=@IsSaving OnClick="Cancel" StartIcon="material-symbols-rounded/close">
                            Close
                        </MudButton>
                    }
                    else if (CRUCMode == CRUDCModes.UPDATE)
                    {
                        <MudButton Variant="Variant.Filled" Size="Size.Small" DropShadow="false" Class="moog-mudbutton" Color="Color.Primary" StartIcon="material-symbols-rounded/save_as"
                                   Disabled=@IsSaving OnClick="@(() => Save(false))">
                            Save
                        </MudButton>
                        <MudButton Variant="Variant.Filled" Size="Size.Small" DropShadow="false" Class="moog-mudbutton" Color="Color.Primary" StartIcon="material-symbols-rounded/save"
                                   Disabled=@IsSaving OnClick="@(() => Save(true))">
                            Save & Close
                        </MudButton>
                        <MudButton Variant="Variant.Filled" Size="Size.Small" DropShadow="false" Class="moog-mudbutton" Color="Color.Default" StartIcon="material-symbols-rounded/cancel"
                                   Disabled=@IsSaving OnClick="Cancel">
                            Cancel
                        </MudButton>
                    }
                    else
                    {
                        <MudButton Variant="Variant.Filled" Size="Size.Small" DropShadow="false" Class="moog-mudbutton" Color="Color.Default" StartIcon="material-symbols-rounded/cancel"
                                   Disabled=@IsSaving OnClick="Cancel">
                            Cancel
                        </MudButton>
                    }
                </MudStack>
            </MudStack>
        </MudPaper>
        <MudTabs Color="Color.Primary" Class="moog-mudtab mt-4" ActiveTabClass="moog-mudtab-tab-active" TabButtonsClass="moog-mudtab-tab-btn" TabHeaderClass="moog-mudtab-tab-hdr" TabPanelsClass="moog-mudtab-tab-panel">
            <MudTabPanel Icon="fad fa-home-lg" Text="Main" Class="moog-mudtabpanel" PanelClass="moog-mudtabpanel-panel">
                <MudPaper Class="moog-mudpaper-cruc-pg-tab-content" Elevation="0" Square="true" Height="calc(100% - var(--moog-cruc-pg-status-bar-height))">
                    <MudStack Row="true" AlignItems="AlignItems.Start" Justify="Justify.SpaceBetween" StretchItems="StretchItems.All" Spacing="1">
                        <MudGrid Spacing="4" Class="pa-4" Style="margin-left:-8px">
                            <MudItem xs="5">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                              Label="Name" @ref=_mtfObjName
                                              @bind-Value="CurrentObject!.ObjectName" For="@(() => CurrentObject!.ObjectName)" />
                            </MudItem>
                            <MudItem xs="1">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true" 
                                              Label="ID" 
                                              @bind-Value="CurrentObject!.ObjectCode" For="@(() => CurrentObject!.ObjectCode)" Style="text-transform:uppercase" />
                            </MudItem>
                            <MudItem xs="2">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="false"
                                              Label="Brand"
                                              @bind-Value="CurrentObject!.Brand" For="@(() => CurrentObject!.Brand)" />
                            </MudItem>
                            <MudItem xs="2">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="false"
                                              Label="Model"
                                              @bind-Value="CurrentObject!.Model" For="@(() => CurrentObject!.Model)" />
                            </MudItem>
                            <MudItem xs="2">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="false"
                                              Label="Serie Name"
                                              @bind-Value="CurrentObject!.SerieName" For="@(() => CurrentObject!.SerieName)" />
                            </MudItem>
                            <MudItem xs="1">
                                <MudDatePicker ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Editable="true" Class="moog-mud-form-field bordered moog-ui-datepicker" Required="false"
                                               AdornmentIcon="material-symbols-rounded/calendar_month" AdornmentColor=@Color.Primary
                                               Label="Release Date" Placeholder="Release Date" DateFormat="dd-MMM-yyyy"
                                               @bind-Date="CurrentObject!.ReleaseDate" For="@(() => CurrentObject!.ReleaseDate)" />
                            </MudItem>
                            <MudItem xs="1">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="false"
                                              Label="Release On"
                                              @bind-Value="CurrentObject!.ReleaseDateText" For="@(() => CurrentObject!.ReleaseDateText)" />
                            </MudItem>
                            <MudItem xs="2">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="false"
                                              Label="Form Factor"
                                              @bind-Value="CurrentObject!.FormFactor" For="@(() => CurrentObject!.FormFactor)" />
                            </MudItem>
                            <MudItem xs="1">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="false"
                                              Label="Dimension (LxWxH)"
                                              @bind-Value="CurrentObject!.DimensionLWH" For="@(() => CurrentObject!.DimensionLWH)" />
                            </MudItem>
                            <MudItem xs="2">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="false"
                                              Label="Recording Technology"
                                              @bind-Value="CurrentObject!.RecordingTech" For="@(() => CurrentObject!.RecordingTech)" />
                            </MudItem>
                            <MudItem xs="1">
                                <MudNumericField ReadOnly=@IsViewMode Clearable="false" ShrinkLabel="true" Class="moog-mud-form-field bordered" Immediate="true" HideSpinButtons="true"
                                                 AdornmentText="MB" Adornment="Adornment.End"
                                                 Label="Cache Size" Step="1"
                                                 @bind-Value="CurrentObject!.CacheSizeMB" T="int?" For="(() => CurrentObject.CacheSizeMB)"
                                                 Style="text-align:right" />
                            </MudItem>
                            <MudItem xs="1">
                                <MudNumericField ReadOnly=@IsViewMode Clearable="false" ShrinkLabel="true" Class="moog-mud-form-field bordered" Immediate="true" HideSpinButtons="true"
                                                 AdornmentText="RPM" Adornment="Adornment.End"
                                                 Label="Rotational Speed" Step="1"
                                                 @bind-Value="CurrentObject!.RotationalSpeed" T="int?" For="(() => CurrentObject.RotationalSpeed)"
                                                 Style="text-align:right" />
                            </MudItem>
                            <MudItem xs="1">
                                <MudNumericField ReadOnly=@IsViewMode Clearable="false" ShrinkLabel="true" Class="moog-mud-form-field bordered" Immediate="true" HideSpinButtons="true"
                                                 AdornmentText="MB" Adornment="Adornment.End"
                                                 Label="Transfer Rate" Step="1"
                                                 @bind-Value="CurrentObject!.TransferRate" T="int?" For="(() => CurrentObject.TransferRate)"
                                                 Style="text-align:right" />
                            </MudItem>
                            <MudItem xs="2">
                                <MudNumericField ReadOnly=@IsViewMode Clearable="false" ShrinkLabel="true" Class="moog-mud-form-field bordered" Immediate="true" HideSpinButtons="true"
                                                 AdornmentText="MB" Adornment="Adornment.End"
                                                 Label="Max Sustained Data Transfer" Step="1"
                                                 @bind-Value="CurrentObject!.MaxSustDTR" T="int?" For="(() => CurrentObject.MaxSustDTR)"
                                                 Style="text-align:right" />
                            </MudItem>
                            <MudItem xs="1">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="false"
                                              Label="Interface"
                                              @bind-Value="CurrentObject!.Interface" For="@(() => CurrentObject!.Interface)" />
                            </MudItem>
                            <MudItem xs="8"></MudItem>
                            <MudItem xs="4">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Lines="8"
                                              Label="Certifications" Placeholder="Note..."
                                              @bind-Value="CurrentObject!.Certification" For="@(() => CurrentObject!.Certification)" />
                            </MudItem>
                            <MudItem xs="4">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Lines="8"
                                              Label="Product Features" Placeholder="Note..."
                                              @bind-Value="CurrentObject!.ProductFeatures" For="@(() => CurrentObject!.ProductFeatures)" />
                            </MudItem>
                            <MudItem xs="4">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Lines="8"
                                              Label="Note" Placeholder="Note..."
                                              @bind-Value="CurrentObject!.Note" For="@(() => CurrentObject!.Note)" />
                            </MudItem>
                            
                            <MudItem xs="6">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered"
                                              AdornmentIcon="material-symbols-rounded/link" Adornment="Adornment.Start" AdornmentColor="@Color.Primary"
                                              Label="Product Info URL"
                                              @bind-Value="CurrentObject!.ProductInfoUrl" For="@(() => CurrentObject!.ProductInfoUrl)" />
                            </MudItem>
                            <MudItem xs="6">
                                <MudTextField ReadOnly=@IsViewMode Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered"
                                              AdornmentIcon="material-symbols-rounded/link" Adornment="Adornment.Start" AdornmentColor="@Color.Primary"
                                              Label="Product Info URL"
                                              @bind-Value="CurrentObject!.TechSpecInfoUrl" For="@(() => CurrentObject!.TechSpecInfoUrl)" />
                            </MudItem>

                            <MudItem xs="12" Class="mt-3"></MudItem>
                            <MudItem xs="12" Style="padding:4px 12px; background-color:var(--ssv-pharma-blue-100)">
                                <MudText Typo="Typo.h1" Style="font-size:18px; font-weight:700; color:var(--ssv-pharma-blue)">Capacity Options Available</MudText>
                            </MudItem>
                            <MudItem xs="12">
                                <MudStack Spacing="2" Justify=Justify.FlexStart>
                                    @if (!IsViewMode)
                                    {
                                        <MudStack Row="true" Spacing="2" Justify=Justify.FlexStart>
                                            <MudButton Variant="Variant.Outlined" Size="Size.Small" Color="Color.Primary" DropShadow="false" FullWidth="false" Class="moog-btn-base moog-btn-tight moog-btn-primary-light" StartIcon="material-symbols-rounded/add_circle"
                                                       OnClick="AddStorageDriveOption" Disabled="!_isEditingStorageDriveOpt">
                                                Add Spec
                                            </MudButton>
                                            <MudSwitchM3 ReadOnly=@IsViewMode Class="moog-mud-switch ml-2" Color="@(_isEditingStorageDriveOpt? Color.Success: Color.Dark)"
                                                         ThumbIcon="@Icons.Material.Rounded.Check" ThumbOffIcon="@Icons.Material.Rounded.Close" Size="Size.Small"
                                                         Label=@(_isEditingStorageDriveOpt ? "Edit ON" : "Edit OFF")
                                                         @bind-Value="_isEditingStorageDriveOpt" Style=@(_isEditingStorageDriveOpt ? "color:#00C853; font-weight:600" : "color:#B0BEC5") />
                                        </MudStack>
                                    }
                                    @* EditDialogOptions=@(new() { BackdropClick = false, MaxWidth = MaxWidth.Medium, FullWidth = true }) *@
                                    <MudDataGrid Class="moog-mud-datagrid" Dense="true" Hover="true" FixedHeader="true" Striped="@(!_isEditingStorageDriveOpt)" HorizontalScrollbar="true" Elevation="0" ColumnResizeMode="ResizeMode.None"
                                                 Items="@CurrentObject!.Options" T="StorageDriveOption" @ref="_storageDriveOptGrid" NoRecordsContent="_gridNoRecord" Style="width:100%" QuickFilter="_quickStorageDriveOptionFilter"
                                                 ReadOnly=@(IsViewMode || !_isEditingStorageDriveOpt) EditTrigger="DataGridEditTrigger.OnRowClick" EditMode="DataGridEditMode.Cell"
                                                 StartedEditingItem="@StartedEditingStorageDriveOption" CanceledEditingItem="@CanceledEditingStorageDriveOption" CommittedItemChanges="@CommittedStorageDriveOptionChanges">
                                        <Columns>
                                            <PropertyColumn Title="No." Property="x => x.SeqNo" Sortable="false" Editable="false" Hidden="true"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt)  ? " spacious" : " data-align-top inline-editing"))
                                                            CellStyle="text-align:right" />
                                            <PropertyColumn Title="Type" Property="x => x.ObjectName" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious" : " data-align-top inline-editing"))
                                                            Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Option Name"
                                                                  @bind-Value=item.Item.ObjectName For="@(() => item.Item.ObjectName)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Capacity" Property="x => x.CapacityText" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious" : " data-align-top inline-editing"))
                                                            Context="item">
                                                <EditTemplate>
                                                    <MudStack Row="true" AlignItems="AlignItems.Start" Justify="Justify.FlexStart" Spacing="2">
                                                        <div style="max-width:80px">
                                                            <MudNumericField ReadOnly=@IsViewMode Clearable="false" ShrinkLabel="true" Class="moog-mud-form-field bordered" Immediate="true" HideSpinButtons="true"
                                                                             Step="1"
                                                                             @bind-Value="item.Item.Capacity" T="int?" For="(() => item.Item.Capacity)"
                                                                             Style="text-align:right" />
                                                        </div>
                                                        <div style="max-width:80px">
                                                            <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                          Placeholder="Unit"
                                                                          @bind-Value=item.Item.CapacityUnit For="@(() => item.Item.CapacityUnit)" OnlyValidateIfDirty="true" />
                                                        </div>
                                                    </MudStack>
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Colors" Property="x => x.Colors" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious" : " data-align-top inline-editing"))
                                                            Context="item">
                                                <EditTemplate>
                                                    <MudStack Row="true" AlignItems="AlignItems.Start" Justify="Justify.FlexStart" Spacing="2">
                                                        <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                      Placeholder="Colors"
                                                                      @bind-Value=item.Item.Colors For="@(() => item.Item.Colors)" OnlyValidateIfDirty="true" />
                                                    </MudStack>
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Warranty" Property="x => x.Warranty" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious" : " data-align-top inline-editing"))
                                                            HeaderStyle="width:300px; max-width:100px" Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  @bind-Value=item.Item.Warranty For="@(() => item.Item.Warranty)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Region" Property="x => x.Region" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious" : " data-align-top inline-editing"))
                                                            HeaderStyle="width:300px; max-width:120px" Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Region"
                                                                  @bind-Value=item.Item.Region For="@(() => item.Item.Region)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Barcode" Property="x => x.Barcode" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious moog-font-mono" : " data-align-top inline-editing"))
                                                            HeaderStyle="width:300px; max-width:150px" Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Barcode"
                                                                  @bind-Value=item.Item.Barcode For="@(() => item.Item.Barcode)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="EAN" Property="x => x.BarcodeEAN" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious moog-font-mono" : " data-align-top inline-editing"))
                                                            HeaderStyle="width:300px; max-width:150px" Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="EAN"
                                                                  @bind-Value=item.Item.BarcodeEAN For="@(() => item.Item.BarcodeEAN)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="UPC" Property="x => x.BarcodeUPC" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious moog-font-mono" : " data-align-top inline-editing"))
                                                            HeaderStyle="width:300px; max-width:150px" Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="UPC"
                                                                  @bind-Value=item.Item.BarcodeUPC For="@(() => item.Item.BarcodeUPC)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Remark" Property="x => x.Remark" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + ((IsViewMode || !_isEditingStorageDriveOpt) ? " spacious" : " data-align-top inline-editing"))
                                                            Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Remark..."
                                                                  @bind-Value=item.Item.Remark For="@(() => item.Item.Remark)" OnlyValidateIfDirty="true" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <TemplateColumn Title="Action" Hidden=@IsViewMode Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass="moog-muddatagrid-cell inline-editing"
                                                            HeaderStyle="min-width:90px" CellStyle="padding-inline-end:4px; padding-inline-start:4px; padding-top: 2px !important; padding-bottom: 2px !important"
                                                            Context="item">
                                                <CellTemplate>
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleUp" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveStorageDriveOptionUp(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleDown" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveStorageDriveOptionDown(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.Delete" Size="@Size.Small" Color="Color.Error" OnClick="@(() => RemoveStorageDriveOption(item.Item))" />
                                                </CellTemplate>
                                                <EditTemplate>
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleUp" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveStorageDriveOptionUp(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleDown" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveStorageDriveOptionDown(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.Delete" Size="@Size.Small" Color="Color.Error" OnClick="@(() => RemoveStorageDriveOption(item.Item))" />
                                                </EditTemplate>
                                            </TemplateColumn>
                                        </Columns>
                                    </MudDataGrid>
                                </MudStack>
                            </MudItem>

                            <MudItem xs="12" Class="mt-3"></MudItem>
                            <MudItem xs="12" Style="padding:4px 12px; background-color:var(--ssv-pharma-blue-100)">
                                <MudText Typo="Typo.h1" Style="font-size:18px; font-weight:700; color:var(--ssv-pharma-blue)">Technical Specifications</MudText>
                            </MudItem>
                            <MudItem xs="12">
                                <MudStack Spacing="2" Justify=Justify.FlexStart>
                                    @if (!IsViewMode)
                                    {
                                        <MudStack Row="true" Spacing="2" Justify=Justify.FlexStart>
                                            <MudButton Variant="Variant.Outlined" Size="Size.Small" Color="Color.Primary" DropShadow="false" FullWidth="false" Class="moog-btn-base moog-btn-tight moog-btn-primary-light" StartIcon="material-symbols-rounded/add_circle"
                                                       OnClick="AddTechSpec" Disabled="!_isEditingTechSpecItem">
                                                Add Spec
                                            </MudButton>
                                            <MudSwitchM3 ReadOnly=@IsViewMode Class="moog-mud-switch ml-2" Color="@(_isEditingTechSpecItem? Color.Success: Color.Dark)"
                                                         ThumbIcon="@Icons.Material.Rounded.Check" ThumbOffIcon="@Icons.Material.Rounded.Close" Size="Size.Small"
                                                         Label=@(_isEditingTechSpecItem ? "Edit ON" : "Edit OFF")
                                                         @bind-Value="_isEditingTechSpecItem" Style=@(_isEditingTechSpecItem ? "color:#00C853; font-weight:600" : "color:#B0BEC5") />
                                        </MudStack>
                                    }
                                    @* EditDialogOptions=@(new() { BackdropClick = false, MaxWidth = MaxWidth.Medium, FullWidth = true }) *@
                                    <MudDataGrid Class="moog-mud-datagrid" Dense="true" Hover="true" FixedHeader="true" Striped="true" HorizontalScrollbar="true" Elevation="0" ColumnResizeMode="ResizeMode.None"
                                                 Items="@CurrentObject!.SpecItems" T="TechSpecItem" @ref="_techSpecGrid" NoRecordsContent="_gridNoRecord" Style="width:100%" QuickFilter="_quickTechSpecFilter"
                                                 ReadOnly=@(IsViewMode || !_isEditingTechSpecItem) EditTrigger="DataGridEditTrigger.OnRowClick" EditMode="DataGridEditMode.Cell"
                                                 StartedEditingItem="@StartedEditingItemSpec" CanceledEditingItem="@CanceledEditingItemSpec" CommittedItemChanges="@CommittedItemSpecChanges">
                                        <Columns>
                                            <PropertyColumn Title="No." Property="x => x.OrderNo" Sortable="false" Editable="false" Hidden="true"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            CellStyle="text-align:right" />
                                            <PropertyColumn Title="Type" Property="x => x.ObjectName" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Spec Type"
                                                                  @bind-Value=item.Item.ObjectName For="@(() => item.Item.ObjectName)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Spec Desc" Property="x => x.SpecDesc" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Spec Value"
                                                                  @bind-Value=item.Item.SpecDesc For="@(() => item.Item.SpecDesc)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Value" Property="x => x.SpecValue" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            HeaderStyle="width:100px; max-width:80px" Context="item">
                                                <EditTemplate>
                                                    <MudNumericField ReadOnly=@IsViewMode Clearable="false" ShrinkLabel="true" Class="moog-mud-form-field bordered" Immediate="true" HideSpinButtons="true"
                                                                     Placeholder="Qty" Format="#,##0.##" Step="0.1M"
                                                                     @bind-Value="item.Item.SpecValue" T="decimal?" For="(() => item.Item.SpecValue)"
                                                                     Style="text-align:right" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="Unit" Property="x => x.UnitCode" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Unit"
                                                                  @bind-Value=item.Item.UnitCode For="@(() => item.Item.UnitCode)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <TemplateColumn Title="Action" Hidden=@IsViewMode Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass="moog-muddatagrid-cell inline-editing"
                                                            CellStyle="padding-inline-end:4px; padding-inline-start:4px; padding-top: 2px !important; padding-bottom: 2px !important"
                                                            Context="item">
                                                <CellTemplate>
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleUp" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveTechSpecUp(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleDown" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveTechSpecDown(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.Delete" Size="@Size.Small" Color="Color.Error" OnClick="@(() => RemoveTechSpec(item.Item))" />
                                                </CellTemplate>
                                                <EditTemplate>
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleUp" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveTechSpecUp(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleDown" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveTechSpecDown(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.Delete" Size="@Size.Small" Color="Color.Error" OnClick="@(() => RemoveTechSpec(item.Item))" />
                                                </EditTemplate>
                                            </TemplateColumn>
                                        </Columns>
                                    </MudDataGrid>
                                </MudStack>
                            </MudItem>

                            <MudItem xs="12" Class="mt-3"></MudItem>
                            <MudItem xs="12" Style="padding:4px 12px; background-color:var(--ssv-pharma-blue-100)">
                                <MudText Typo="Typo.h1" Style="font-size:18px; font-weight:700; color:var(--ssv-pharma-blue)">IO Port(s)</MudText>
                            </MudItem>
                            <MudItem xs="12">
                                <MudStack Spacing="2" Justify=Justify.FlexStart>
                                    @if (!IsViewMode)
                                    {
                                        <MudStack Row="true" Spacing="2" Justify=Justify.FlexStart>
                                            <MudButton Variant="Variant.Outlined" Size="Size.Small" Color="Color.Primary" DropShadow="false" FullWidth="false" Class="moog-btn-base moog-btn-tight moog-btn-primary-light" StartIcon="material-symbols-rounded/add_circle"
                                                       OnClick="AddIOPort" Disabled="!_isEditingIOPort">
                                                Add Spec
                                            </MudButton>
                                            <MudSwitchM3 ReadOnly=@IsViewMode Class="moog-mud-switch ml-2" Color="@(_isEditingIOPort? Color.Success: Color.Dark)"
                                                         ThumbIcon="@Icons.Material.Rounded.Check" ThumbOffIcon="@Icons.Material.Rounded.Close" Size="Size.Small"
                                                         Label=@(_isEditingIOPort ? "Edit ON" : "Edit OFF")
                                                         @bind-Value="_isEditingIOPort" Style=@(_isEditingIOPort ? "color:#00C853; font-weight:600" : "color:#B0BEC5") />
                                        </MudStack>
                                    }
                                    @* EditDialogOptions=@(new() { BackdropClick = false, MaxWidth = MaxWidth.Medium, FullWidth = true }) *@
                                    <MudDataGrid Class="moog-mud-datagrid" Dense="true" Hover="true" FixedHeader="true" Striped="true" HorizontalScrollbar="true" Elevation="0" ColumnResizeMode="ResizeMode.None"
                                                 Items="@CurrentObject!.IOPorts" T="DeviceIOPort" @ref="_ioPortGrid" NoRecordsContent="_gridNoRecord" QuickFilter="_quickIOPortFilter"
                                                 ReadOnly=@(IsViewMode || !_isEditingIOPort) EditTrigger="DataGridEditTrigger.OnRowClick" EditMode="DataGridEditMode.Cell"
                                                 StartedEditingItem="@StartedEditingIOPort" CanceledEditingItem="@CanceledEditingIOPort" CommittedItemChanges="@CommittedIOPortChanges">
                                        <Columns>
                                            <PropertyColumn Title="No." Property="x => x.SeqNo" Sortable="false" Editable="false" Hidden="true"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            CellStyle="text-align:right" />
                                            <PropertyColumn Title="Type" Property="x => x.ObjectName" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            Context="item">
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Spec Type"
                                                                  @bind-Value=item.Item.ObjectName For="@(() => item.Item.ObjectName)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <PropertyColumn Title="# Port" Property="x => x.PortCount" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            HeaderStyle="width:500px; max-width:80px"
                                                            CellStyle="text-align:right" Context="item">
                                                <EditTemplate>
                                                    <MudNumericField ReadOnly=@IsViewMode Clearable="false" ShrinkLabel="true" Class="moog-mud-form-field bordered" Immediate="true" HideSpinButtons="true"
                                                                     Format="#,##0" Step="1"
                                                                     @bind-Value="item.Item.PortCount" T="int?" For="(() => item.Item.PortCount)"
                                                                     Style="text-align:right" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            
                                            <PropertyColumn Title="Remark" Property="x => x.Remark" Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass=@("moog-muddatagrid-cell" + (IsViewMode ? " spacious" : " data-align-top inline-editing"))
                                                            HeaderStyle="width:800px" Context="item" >
                                                <EditTemplate>
                                                    <MudTextField Readonly="@IsViewMode" Clearable="true" ShrinkLabel="true" Class="moog-mud-form-field bordered" Required="true"
                                                                  Placeholder="Remark..."
                                                                  @bind-Value=item.Item.Remark For="@(() => item.Item.Remark)" OnlyValidateIfDirty="true" MaxLength="255" />
                                                </EditTemplate>
                                            </PropertyColumn>
                                            <TemplateColumn Title="Action" Hidden=@IsViewMode Sortable="false"
                                                            HeaderClass="moog-mud-datagrid-hdr cruc-pg-items" CellClass="moog-muddatagrid-cell inline-editing"
                                                            CellStyle="padding-inline-end:4px; padding-inline-start:4px; padding-top: 2px !important; padding-bottom: 2px !important"
                                                            Context="item">
                                                <CellTemplate>
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleUp" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveIOPortUp(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleDown" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveIOPortDown(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.Delete" Size="@Size.Small" Color="Color.Error" OnClick="@(() => RemoveIOPort(item.Item))" />
                                                </CellTemplate>
                                                <EditTemplate>
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleUp" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveIOPortUp(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.ArrowCircleDown" Size="@Size.Small" Color="Color.Primary" OnClick="@(() => MoveIOPortDown(item.Item))" />
                                                    <MudIconButton Class="moog-ico-btn-datagrid-action" Icon="@Icons.Material.Outlined.Delete" Size="@Size.Small" Color="Color.Error" OnClick="@(() => RemoveIOPort(item.Item))" />
                                                </EditTemplate>
                                            </TemplateColumn>
                                        </Columns>
                                    </MudDataGrid>
                                </MudStack>
                            </MudItem>
                            @if (CurrentEditContext.GetValidationMessages().Any() || InvalidMsgList.Count > 0)
                            {
                                <MudItem xs="12" Class="pt-4">
                                    @foreach (string msg in CurrentEditContext.GetValidationMessages())
                                    {
                                        <MudAlert Variant="Variant.Outlined" Severity="Severity.Warning" Icon="fad fa-exclamation-circle" Class="moog-alert-validation mt-2">@msg</MudAlert>
                                    }
                                    @foreach (string key in InvalidMsgList.Keys)
                                    {
                                        <MudAlert Variant="Variant.Outlined" Severity="Severity.Warning" Icon="fad fa-exclamation-circle" Class="moog-alert-validation mt-2">@InvalidMsgList[key]</MudAlert>
                                    }
                                </MudItem>
                            }
                        </MudGrid>
                    </MudStack>
                </MudPaper>
                <MudPaper Class="moog-mudpaper-cruc-pg-status" Height="var(--moog-cruc-pg-status-bar-height)" Elevation="0">
                    <MudStack Row="true" AlignItems="AlignItems.Center" Justify="Justify.FlexStart" Spacing="1" Style="height:100%; padding: 0px 16px">
                        @if (CurrentObject.Id > 0)
                        {
                            <MudText Typo="Typo.subtitle1" Color="Color.Default">Created by </MudText>
                            <MudText Typo="Typo.subtitle1" Color="Color.Primary" Style="font-weight:600">@CurrentObject!.CreatedUser</MudText>
                            <MudText Typo="Typo.subtitle1" Color="Color.Default"> on </MudText>
                            <MudText Typo="Typo.subtitle1" Color="Color.Primary" Style="font-weight:600">@(CurrentObject!.CreatedDateTimeText)</MudText>
                            <MudText Typo="Typo.subtitle1" Color="Color.Default">| Last modified by </MudText>
                            <MudText Typo="Typo.subtitle1" Color="Color.Primary" Style="font-weight:600">@CurrentObject!.ModifiedUser</MudText>
                            <MudText Typo="Typo.subtitle1" Color="Color.Default"> on </MudText>
                            <MudText Typo="Typo.subtitle1" Color="Color.Primary" Style="font-weight:600">@(CurrentObject!.ModifiedDateTimeText)</MudText>
                        }
                    </MudStack>
                </MudPaper>
            </MudTabPanel>
            <MudTabPanel Icon="fad fa-circle-info" Text="Other Info" Class="moog-mudtabpanel" PanelClass="moog-mudtabpanel-panel">
                <MudPaper Class="moog-mudpaper-cruc-pg-tab-content" Elevation="0" Square="true" Height="calc(100% - var(--moog-cruc-pg-status-bar-height))">
                    <MudGrid Spacing="3" Class="pa-3">
                        <MudItem xs="12" Style="padding:4px 12px; background-color:var(--ssv-pharma-blue-100)">
                            
                        </MudItem>
                        <MudItem xs="12">
                            
                        </MudItem>
                    </MudGrid>
                </MudPaper>
            </MudTabPanel>
        </MudTabs>
    </EditForm>
}

@code {

    #region UI PARAM
    RenderFragment _gridNoRecord = @<p>No record</p>;
    
    List<DropDownListItem> _countryList = [];

    IEnumerable<string> _brandList = [];
    MudTextField<string>? _mtfObjName;
    List<AttachedImage> _images = [];
    
    List<DropdownSelectItem> _currencyList = [];

    MudDataGrid<TechSpecItem> _techSpecGrid = new();
    TechSpecItem? _tempTechSpecToAdd;
    bool _isEditingTechSpecItem = true;

    MudDataGrid<DeviceIOPort> _ioPortGrid = new();
    DeviceIOPort? _tempIOPortToAdd;
    bool _isEditingIOPort = true;

    MudDataGrid<StorageDriveOption> _storageDriveOptGrid = new();
    StorageDriveOption? _tempStorageDriveOptToAdd;
    bool _isEditingStorageDriveOpt = true;
    #endregion

    #region PAGE LIFECYCLE FUNCTIONS
    public override Task SetParametersAsync(ParameterView parameters)
    {
        UrlPrefix = "/tsm/hdd";
        return base.SetParametersAsync(parameters);
    }

    protected override async Task OnInitializedAsync()
    {
        switch (CRUCMode)
        {
            case CRUDCModes.READ:
                {
                    CurrentObject = await UowPg.HDDs.GetAsync(Id);
                    IsViewMode = true;
                }
                break;
            case CRUDCModes.CREATE:
                {
                    CurrentObject = new();
                }
                break;
            case CRUDCModes.UPDATE:
                CurrentObject = await UowPg.HDDs.GetAsync(Id);
                break;
            case CRUDCModes.CLONE:
                {
                    var tempObj = await UowPg.HDDs.GetAsync(Id);
                    CurrentObject = tempObj!.DeepClone();

                    CurrentObject!.Id = 0;
                }
                break;
        }

        if (CurrentObject != null && CurrentObject.Id > 0)
        {
            CurrentObject.SpecItems = await UowPg.TechSpecItems.GetByObjectAsync(CurrentObject.GetType().Name, CurrentObject.Id);
            CurrentObject.IOPorts = await UowPg.DeviceIOPorts.GetByObjectAsync(CurrentObject.GetType().Name, CurrentObject.Id);
            CurrentObject.Options = await UowPg.StorageDriveOptions.GetByDriveAsync(StorageDriveTypes.HDD, CurrentObject.Id);
        }

        CurrentEditContext = new EditContext(CurrentObject!);

        await PopulatedUIControls();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            if (_mtfObjName != null && !IsViewMode)
                await _mtfObjName!.FocusAsync();
        }
    }
    #endregion

    #region UI FUNCTIONS
    async Task<IEnumerable<string>> SearchBrandName(string value, CancellationToken token)
    {
        // In real life use an asynchronous function for fetching data from an api.
        await Task.Delay(5, token);

        if (string.IsNullOrEmpty(value))
            return _brandList;
        else
            return _brandList.Where(x => x.Contains(value, StringComparison.OrdinalIgnoreCase));
    }
    #endregion

    #region Storage Drive Options
    // quick filter - filter globally across multiple columns with the same input
    private Func<StorageDriveOption, bool> _quickStorageDriveOptionFilter => x =>
    {
        return !x.IsDeleted;
    };

    void AddStorageDriveOption()
    {
        _tempStorageDriveOptToAdd = new()
        {
            SeqNo = CurrentObject!.Options.Count + 1
        };

        CurrentObject!.Options.Add(_tempStorageDriveOptToAdd);
        // _itemDataGrid.SetEditingItemAsync(_tempItemToAdd);
    }

    void MoveStorageDriveOptionUp(StorageDriveOption sdo)
    {
        if (CurrentObject!.Options.Count > 1 && sdo.SeqNo!.Value > 1)
        {
            sdo.SeqNo--;

            StorageDriveOption? a = CurrentObject!.Options.FirstOrDefault(x => x.IsDeleted == false && x.SeqNo is not null && x.SeqNo == sdo.SeqNo && x != sdo);

            if (a != null)
                a!.SeqNo++;

            CurrentObject!.Options = CurrentObject!.Options.OrderBy(x => x.SeqNo).ToList();
        }
    }

    void MoveStorageDriveOptionDown(StorageDriveOption sdo)
    {
        if (CurrentObject!.Options.Count > 1 && CurrentObject!.Options.Count != sdo.SeqNo!.Value)
        {
            sdo.SeqNo++;

            StorageDriveOption? a = CurrentObject!.Options.FirstOrDefault(x => x.IsDeleted == false && x.SeqNo is not null && x.SeqNo == sdo.SeqNo && x != sdo);

            if (a != null)
                a!.SeqNo--;

            CurrentObject!.Options = CurrentObject!.Options.OrderBy(x => x.SeqNo).ToList();
        }
    }

    void RemoveStorageDriveOption(StorageDriveOption sdo)
    {
        if (sdo.Id == 0)
        {
            CurrentObject!.Options.Remove(sdo);

            foreach (var var in CurrentObject!.Options.Where(x => !x.IsDeleted && x.SeqNo > sdo.SeqNo))
            {
                var.SeqNo--;
            }
        }
        else if (sdo.Id > 0)
        {
            foreach (var var in CurrentObject!.Options.Where(x => !x.IsDeleted && x.SeqNo > sdo.SeqNo))
                var.SeqNo--;

            sdo.SeqNo = null;
            sdo.IsDeleted = true;
        }
    }

    void StartedEditingStorageDriveOption(StorageDriveOption ioPort)
    {

    }

    void CanceledEditingStorageDriveOption(StorageDriveOption ioPort)
    {
        if (_tempStorageDriveOptToAdd != null)
        {
            CurrentObject!.Options.RemoveAt(CurrentObject!.Options.FindIndex(x => x.SeqNo == ioPort.SeqNo));
            _tempStorageDriveOptToAdd = null;
        }
    }

    Task<DataGridEditFormAction> CommittedStorageDriveOptionChanges(StorageDriveOption ioPort)
    {
        _tempStorageDriveOptToAdd = null;

        return Task.FromResult(DataGridEditFormAction.Close);
    }
    #endregion

    #region Tech Specification
    // quick filter - filter globally across multiple columns with the same input
    private Func<TechSpecItem, bool> _quickTechSpecFilter => x =>
    {
        return !x.IsDeleted;
    };

    void AddTechSpec()
    {
        _tempTechSpecToAdd = new()
        {
            OrderNo = CurrentObject!.SpecItems.Count + 1
        };

        CurrentObject!.SpecItems.Add(_tempTechSpecToAdd);
        // _itemDataGrid.SetEditingItemAsync(_tempItemToAdd);
    }

    void MoveTechSpecUp(TechSpecItem techSpec)
    {
        if (CurrentObject!.SpecItems.Count > 1 && techSpec.OrderNo!.Value > 1)
        {
            techSpec.OrderNo--;

            TechSpecItem? a = CurrentObject!.SpecItems.FirstOrDefault(x => x.IsDeleted == false && x.OrderNo is not null && x.OrderNo == techSpec.OrderNo && x != techSpec);

            if (a != null)
                a!.OrderNo++;

            CurrentObject!.SpecItems = CurrentObject!.SpecItems.OrderBy(x => x.OrderNo).ToList();
        }
    }

    void MoveTechSpecDown(TechSpecItem techSpec)
    {
        if (CurrentObject!.SpecItems.Count > 1 && CurrentObject!.SpecItems.Count != techSpec.OrderNo!.Value)
        {
            techSpec.OrderNo++;

            TechSpecItem? a = CurrentObject!.SpecItems.FirstOrDefault(x => x.IsDeleted == false && x.OrderNo is not null && x.OrderNo == techSpec.OrderNo && x != techSpec);

            if (a != null)
                a!.OrderNo--;

            CurrentObject!.SpecItems = CurrentObject!.SpecItems.OrderBy(x => x.OrderNo).ToList();
        }
    }

    void RemoveTechSpec(TechSpecItem techSpec)
    {
        if (techSpec.Id == 0)
        {
            CurrentObject!.SpecItems.Remove(techSpec);

            foreach (var var in CurrentObject!.SpecItems.Where(x => !x.IsDeleted && x.OrderNo > techSpec.OrderNo))
            {
                var.OrderNo--;
            }
        }
        else if (techSpec.Id > 0)
        {
            foreach (var var in CurrentObject!.SpecItems.Where(x => !x.IsDeleted && x.OrderNo > techSpec.OrderNo))
                var.OrderNo--;

            techSpec.OrderNo = null;
            techSpec.IsDeleted = true;
        }
    }

    void StartedEditingItemSpec(TechSpecItem techSpec)
    {

    }

    void CanceledEditingItemSpec(TechSpecItem techSpec)
    {
        if (_tempTechSpecToAdd != null)
        {
            CurrentObject!.SpecItems.RemoveAt(CurrentObject!.SpecItems.FindIndex(x => x.OrderNo == techSpec.OrderNo));
            _tempTechSpecToAdd = null;
        }
    }

    Task<DataGridEditFormAction> CommittedItemSpecChanges(TechSpecItem techSpec)
    {
        _tempTechSpecToAdd = null;

        return Task.FromResult(DataGridEditFormAction.Close);
    }
    #endregion

    #region IO Port
    // quick filter - filter globally across multiple columns with the same input
    private Func<DeviceIOPort, bool> _quickIOPortFilter => x =>
    {
        return !x.IsDeleted;
    };

    void AddIOPort()
    {
        _tempIOPortToAdd = new()
        {
            SeqNo = CurrentObject!.IOPorts.Count + 1
        };

        CurrentObject!.IOPorts.Add(_tempIOPortToAdd);
        // _itemDataGrid.SetEditingItemAsync(_tempItemToAdd);
    }

    void MoveIOPortUp(DeviceIOPort ioPort)
    {
        if (CurrentObject!.IOPorts.Count > 1 && ioPort.SeqNo!.Value > 1)
        {
            ioPort.SeqNo--;

            DeviceIOPort? a = CurrentObject!.IOPorts.FirstOrDefault(x => x.IsDeleted == false && x.SeqNo is not null && x.SeqNo == ioPort.SeqNo && x != ioPort);

            if (a != null)
                a!.SeqNo++;

            CurrentObject!.IOPorts = CurrentObject!.IOPorts.OrderBy(x => x.SeqNo).ToList();
        }
    }

    void MoveIOPortDown(DeviceIOPort ioPort)
    {
        if (CurrentObject!.IOPorts.Count > 1 && CurrentObject!.IOPorts.Count != ioPort.SeqNo!.Value)
        {
            ioPort.SeqNo++;

            DeviceIOPort? a = CurrentObject!.IOPorts.FirstOrDefault(x => x.IsDeleted == false && x.SeqNo is not null && x.SeqNo == ioPort.SeqNo && x != ioPort);

            if (a != null)
                a!.SeqNo--;

            CurrentObject!.IOPorts = CurrentObject!.IOPorts.OrderBy(x => x.SeqNo).ToList();
        }
    }

    void RemoveIOPort(DeviceIOPort ioPort)
    {
        if (ioPort.Id == 0)
        {
            CurrentObject!.IOPorts.Remove(ioPort);

            foreach (var var in CurrentObject!.IOPorts.Where(x => !x.IsDeleted && x.SeqNo > ioPort.SeqNo))
            {
                var.SeqNo--;
            }
        }
        else if (ioPort.Id > 0)
        {
            foreach (var var in CurrentObject!.IOPorts.Where(x => !x.IsDeleted && x.SeqNo > ioPort.SeqNo))
                var.SeqNo--;

            ioPort.SeqNo = null;
            ioPort.IsDeleted = true;
        }
    }

    void StartedEditingIOPort(DeviceIOPort ioPort)
    {

    }

    void CanceledEditingIOPort(DeviceIOPort ioPort)
    {
        if (_tempIOPortToAdd != null)
        {
            CurrentObject!.IOPorts.RemoveAt(CurrentObject!.IOPorts.FindIndex(x => x.SeqNo == ioPort.SeqNo));
            _tempIOPortToAdd = null;
        }
    }

    Task<DataGridEditFormAction> CommittedIOPortChanges(DeviceIOPort ioPort)
    {
        _tempIOPortToAdd = null;

        return Task.FromResult(DataGridEditFormAction.Close);
    }
    #endregion

    #region DB UPDATE FUNCTIONS
    protected override async Task ValidatePreSave()
    {
        InvalidMsgList.Clear();

        bool isDuplicated = await UowPg.HDDs.IsDuplicateCodeAsync(CurrentObject!.Id, CurrentObject!.ObjectCode!);

        if (isDuplicated)
        {
            InvalidMsgList.Add("ObjectCode_Duplicate", $"Record with ObjectCode='{CurrentObject.ObjectCode}' is already existed.");
        }
    }

    protected override Task PreSaveProcessing()
    {
        CurrentObject!.ObjectCode = CurrentObject!.ObjectCode!.ToUpper();

        if (CurrentObject!.ReleaseDate.HasValue)
        {
            CurrentObject!.ReleaseDateText = CurrentObject!.ReleaseDate!.Value.ToString("yyyy-MM-dd");
        }

        return base.PreSaveProcessing();
    }

    protected override async Task<bool> SaveAndCommitProcessing()
    {
        DateTime timestamp = DateTime.Now;

        if (CurrentObject!.Id == 0)
        {
            CurrentObject!.CreatedUser = AuditTrailUser;
            CurrentObject!.CreatedDateTime = timestamp;
            CurrentObject!.ModifiedUser = AuditTrailUser;
            CurrentObject!.ModifiedDateTime = timestamp;
            int objId = await UowPg.HDDs.ManualInsertAsync(HDD.GetPgFieldList(UowPg.DbContext.DbType), CurrentObject.GetParamValues(UowPg.DbContext.DbType));

            if (objId > 0)
            {
                foreach (TechSpecItem specItem in CurrentObject.SpecItems)
                {
                    if (specItem.IsDeleted) continue;

                    specItem.LinkedObjectId = objId;
                    specItem.LinkedObjectType = CurrentObject.GetType().Name;
                    specItem.CreatedDateTime = CurrentObject.CreatedDateTime;
                    specItem.CreatedUser = CurrentObject.CreatedUser;
                    specItem.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                    specItem.ModifiedUser = CurrentObject.ModifiedUser;
                    int specItemId = await UowPg.TechSpecItems.ManualInsertAsync(TechSpecItem.GetPgFieldList(UowPg.DbContext.DbType), specItem.GetParamValues(UowPg.DbContext.DbType));
                }

                foreach (DeviceIOPort ioPort in CurrentObject.IOPorts)
                {
                    if (ioPort.IsDeleted) continue;

                    ioPort.LinkedObjectId = objId;
                    ioPort.LinkedObjectCode = CurrentObject.ObjectCode;
                    ioPort.LinkedObjectType = CurrentObject.GetType().Name;
                    ioPort.CreatedDateTime = CurrentObject.CreatedDateTime;
                    ioPort.CreatedUser = CurrentObject.CreatedUser;
                    ioPort.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                    ioPort.ModifiedUser = CurrentObject.ModifiedUser;

                    int ioPortId = await UowPg.DeviceIOPorts.ManualInsertAsync(DeviceIOPort.GetPgFieldList(UowPg.DbContext.DbType), ioPort.GetParamValues(UowPg.DbContext.DbType));
                }

                foreach (StorageDriveOption sdo in CurrentObject.Options)
                {
                    if (sdo.IsDeleted) continue;

                    sdo.StorageType = StorageDriveTypes.HDD;
                    sdo.StorageDriveId = CurrentObject.Id;
                    sdo.CreatedDateTime = CurrentObject.CreatedDateTime;
                    sdo.CreatedUser = CurrentObject.CreatedUser;
                    sdo.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                    sdo.ModifiedUser = CurrentObject.ModifiedUser;

                    int sdoId = await UowPg.StorageDriveOptions.ManualInsertAsync(StorageDriveOption.GetPgFieldList(UowPg.DbContext.DbType), sdo.GetParamValues(UowPg.DbContext.DbType));
                }
            }

            return objId > 0;
        }
        else if (CurrentObject!.Id > 0)
        {
            CurrentObject!.ModifiedUser = AuditTrailUser;
            CurrentObject!.ModifiedDateTime = timestamp;
            int updCount = await UowPg.HDDs.ManualUpdateAsync(HDD.GetPgFieldList(UowPg.DbContext.DbType), CurrentObject.GetParamValues(UowPg.DbContext.DbType, true));

            if (updCount > 0)
            {
                foreach (TechSpecItem specItem in CurrentObject.SpecItems)
                {
                    if (specItem.Id == 0 & specItem.IsDeleted) continue;

                    if (specItem.Id > 0)
                    {
                        specItem.LinkedObjectId = CurrentObject.Id;
                        specItem.LinkedObjectType = CurrentObject.GetType().Name;
                        specItem.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                        specItem.ModifiedUser = CurrentObject.ModifiedUser;

                        int specUpdCount = await UowPg.TechSpecItems.ManualUpdateAsync(TechSpecItem.GetPgFieldList(UowPg.DbContext.DbType), specItem.GetParamValues(UowPg.DbContext.DbType, true));
                    }
                    else if (specItem.Id == 0)
                    {
                        specItem.LinkedObjectId = CurrentObject.Id;
                        specItem.LinkedObjectType = CurrentObject.GetType().Name;
                        specItem.CreatedDateTime = CurrentObject.ModifiedDateTime;
                        specItem.CreatedUser = CurrentObject.ModifiedUser;
                        specItem.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                        specItem.ModifiedUser = CurrentObject.ModifiedUser;

                        int specItemId = await UowPg.TechSpecItems.ManualInsertAsync(TechSpecItem.GetPgFieldList(UowPg.DbContext.DbType), specItem.GetParamValues(UowPg.DbContext.DbType));
                    }
                }

                foreach (DeviceIOPort ioPort in CurrentObject.IOPorts)
                {
                    if (ioPort.Id == 0 & ioPort.IsDeleted) continue;

                    if (ioPort.Id > 0)
                    {
                        ioPort.LinkedObjectId = CurrentObject.Id;
                        ioPort.LinkedObjectType = CurrentObject.GetType().Name;
                        ioPort.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                        ioPort.ModifiedUser = CurrentObject.ModifiedUser;
                        int ioPortUpdCount = await UowPg.DeviceIOPorts.ManualUpdateAsync(DeviceIOPort.GetPgFieldList(UowPg.DbContext.DbType), ioPort.GetParamValues(UowPg.DbContext.DbType, true));
                    }
                    else if (ioPort.Id == 0)
                    {
                        ioPort.LinkedObjectId = CurrentObject.Id;
                        ioPort.LinkedObjectType = CurrentObject.GetType().Name;
                        ioPort.CreatedDateTime = CurrentObject.ModifiedDateTime;
                        ioPort.CreatedUser = CurrentObject.ModifiedUser;
                        ioPort.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                        ioPort.ModifiedUser = CurrentObject.ModifiedUser;

                        int ioPortId = await UowPg.DeviceIOPorts.ManualInsertAsync(DeviceIOPort.GetPgFieldList(UowPg.DbContext.DbType), ioPort.GetParamValues(UowPg.DbContext.DbType));
                    }
                }

                foreach (StorageDriveOption sdo in CurrentObject.Options)
                {
                    if (sdo.Id == 0 & sdo.IsDeleted) continue;

                    if (sdo.Id > 0)
                    {
                        sdo.StorageType = StorageDriveTypes.HDD;
                        sdo.StorageDriveId = CurrentObject.Id;
                        sdo.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                        sdo.ModifiedUser = CurrentObject.ModifiedUser;
                        int sdoUpdCount = await UowPg.StorageDriveOptions.ManualUpdateAsync(StorageDriveOption.GetPgFieldList(UowPg.DbContext.DbType), sdo.GetParamValues(UowPg.DbContext.DbType, true));
                    }
                    else if (sdo.Id == 0)
                    {
                        sdo.StorageType = StorageDriveTypes.HDD;
                        sdo.StorageDriveId = CurrentObject.Id;
                        sdo.CreatedDateTime = CurrentObject.ModifiedDateTime;
                        sdo.CreatedUser = CurrentObject.ModifiedUser;
                        sdo.ModifiedDateTime = CurrentObject.ModifiedDateTime;
                        sdo.ModifiedUser = CurrentObject.ModifiedUser;

                        int sdoId = await UowPg.StorageDriveOptions.ManualInsertAsync(StorageDriveOption.GetPgFieldList(UowPg.DbContext.DbType), sdo.GetParamValues(UowPg.DbContext.DbType));
                    }
                }
            }

            return updCount > 0;
        }
        else
            return false;
    }
    #endregion

    #region INTERNAL FUNCTIONS
    async Task PopulatedUIControls()
    {
        _brandList = await Uow.Brands.GetAllBrandNamesAsync();
        _countryList = await Uow.Countries.GetForDropdownSelect1Async();
        _currencyList = await Uow.Currencies.GetForDropdownSelect1Async(null);
    }
    #endregion
}
